# Environment names whose presence means this shell can reach live data or a live
# surface. tools/verify_seam.sh refuses to run a verify while any of them is set.
#
# THIS IS AN ALLOWLIST BY OMISSION, WHICH IS WHY IT HAS A TEST. A credential-shaped
# name that lives in .env or gets resolved by a script, and isn't listed here,
# defeats the guard silently: the verify reports green while its writes land on
# production. tools/tests/test_verify_seam_credentials.sh scans for
# credential-shaped names and fails on any it can't find here. Expect that
# failure the first time a new secret appears in this repo. That failure is the
# feature — when it fires, add the new name below rather than silencing the test.
#
# Seeded with generic, widely-applicable credential names plus the two this
# repo's own voice channel resolves.

# Cloud provider credentials
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
GCP_SERVICE_ACCOUNT_KEY
AZURE_CLIENT_SECRET

# Databases, in common spellings
DATABASE_URL
POSTGRES_URL
REDIS_URL

# Model-provider keys
OPENAI_API_KEY
ANTHROPIC_API_KEY

# Payment-provider keys
STRIPE_SECRET_KEY
STRIPE_API_KEY

# Generic OAuth client credentials
OAUTH_CLIENT_ID
OAUTH_CLIENT_SECRET
REFRESH_TOKEN

# The loop's voice channel (scripts/loop_voice.py), which reads Fish and Telegram
# credentials from pointer files (~/.config/fish-audio/speak.json,
# ~/.config/loop-voice/telegram.json), never env. Listed here so the seam refuses
# a shell that has exported them anyway.
FISH_API_KEY
FISH_AUDIO_API_KEY
TELEGRAM_BOT_TOKEN
TELEGRAM_CHAT_ID
